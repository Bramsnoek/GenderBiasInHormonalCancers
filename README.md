# GenderBiasInHormonalCancers


To run the pipeline download the pipeline folder from the master branch. 
Navigate to the folder using the commandline. To run the pipeline enter "bash nextflow pipeline.nf"
